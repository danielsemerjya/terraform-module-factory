# All outputs live here. Every output needs a description. Minimum: id and name of
# every resource created; for_each resources as maps keyed like the input
# (standards/variables-outputs.md).

# output "id" {
#   description = "Resource ID of the TODO."
#   value       = azurerm_TODO.this.id
# }
