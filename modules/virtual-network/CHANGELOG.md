# Changelog

All notable changes to this module. Format follows
[Keep a Changelog](https://keepachangelog.com); versioning rules in
`standards/versioning.md`.

## [Unreleased]

### Added

- Initial implementation: virtual network with address space, custom DNS servers and
  tags; subnets via `for_each` map with service endpoints, delegation, secure defaults
  (private-endpoint-ready, default outbound access disabled).
